/**
 * context api
 * 1.creater context (export)
 * 2.create provider
 * 3.set context value 
 * 4. to acces the vlaue : usecontext hook
 */

/**
 * 1.mian.jsx set AuthProvider
 * 2. inside provider access children props and then use
 */

/**
 * 1.break down the create user function
 * 2.
 */